{
  "name": "privy-validator",
  "version": "1.0.5",
  "description": "validator express",
  "license": "MIT",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "ts-node ./src/test/index.test.ts",
    "release:patch": "npm run build && git add . && git commit && npm version patch",
    "release:minor": "npm run build && git add . && git commit && npm version minor",
    "release:major": "npm run build && git add . && git commit && npm version major"
  },
  "engines": {
    "node": ">=14.20.1"
  },
  "keywords": [
    "express",
    "validator",
    "express validator",
    "request validator",
    "request body validator",
    "simple validator"
  ],
  "devDependencies": {
    "@types/node": "^20.7.0"
  }
}
